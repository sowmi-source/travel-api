
# apiVersion: v1
# kind: Service
# metadata:
#   name: travel-api-service
#   namespace: travel
# spec:
#   selector:
#     app: travel-new
#   ports:
#     - protocol: TCP
#       port: 8443
#       targetPort: 8443
#   type: ClusterIP   # nodeport - 30000-32767


# apiVersion: v1
# kind: Service
# metadata:
#   name: travel-api-service
#   namespace: new-travel-api
# spec:
#   selector:
#     app: travel-api
    
#   ports:
#     - protocol: TCP
#       port: 8442         # Service port (HTTPS)
#       targetPort: 8442  # Container port
#   nodePort: 30080   # NodePort for external access
#   selector:
#     app: travel-api

apiVersion: v1
kind: Service
metadata:
  name: travel-api-service
spec:
  type: NodePort
  selector:
    app: travel-api
  ports:
    - port: 8442
      targetPort: 8442
      nodePort: 30080 





